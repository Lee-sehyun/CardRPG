#include <iostream>
#include <vector>

using namespace std;

class Skill {
    private:
        vector<string> skillName;
        vector<string> skillDescription;
        vector<int> skillProbability;
};